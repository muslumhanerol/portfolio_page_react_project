import React from "react";
import { projects } from "../data/projects";

const Projects = ({ hasAnimated }) => {
    return (
        <section id="projects" className={`py-16 px-6 bg-white`}></section>
    )

}

export default Projects;